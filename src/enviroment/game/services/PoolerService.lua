local Instance = require("@Instance")
local signal = require("@Kinemium.signal")

local PoolerService = Instance.new("PoolerService")
PoolerService.ExplorerHidden = true

-- nothing to implement for now..

return PoolerService
